"use strict";
/*
export class CookieCollection extends DictCollection<string> {
    
    private loadCookies() {
        var cookies = document.cookie.split(';');
        for (var i = 0; i < cookies.length;i++) {
            var cookie = cookies[i].split('=');
            if (cookie.length == 2) {
                super.set(cookie[0].trim(), cookie[1].trim());
            }
        }
    }
    
    public constructor() {
        super();
        this.loadCookies();
    }

    public set(name: string, value: string) {
        document.cookie = `${name}=${value}`;
        super.set(name, value);
    }
}
*/
//# sourceMappingURL=cookieCollection.js.map